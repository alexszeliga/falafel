<h1>Falafel</h1>

<h2>Falafel on the menu</h2>

<ul>
    {{#each falafel}}
    {{#unless eaten}}
    {{> falafel/menu-item eat=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Falafel you've eaten</h2>
<ul>
    {{#each falafel}}
    {{#if eaten}}
    {{> falafel/menu-item eat=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Falafel</h2>
{{!-- TODO: Falafel Form --}}
<form class="create-form">

    <div class="form-group">
        <label for="falafel">Falafel Type:</label>
        <input type="text" id="falafel" name="name">
    </div>

    <button type="submit">Add Cat</button>
</form>